# Local Development - Using standard PostgreSQL connection
# Uncomment these for local testing with Docker
DATABASE_URL=postgresql+psycopg://user_blacklist:password123@localhost:5433/blacklist_db
JWT_SECRET_KEY=clave-secreta-uniandes-2026

# ============================================================
# AWS RDS Configuration (Uncomment for AWS RDS deployment)
# ============================================================
# Leave DATABASE_URL commented out when using RDS_HOSTNAME
# DATABASE_URL=

# AWS RDS Endpoint (e.g., database-1.cluster-xxxxx.us-east-1.rds.amazonaws.com)
# RDS_HOSTNAME=your-rds-endpoint.us-east-1.rds.amazonaws.com
# RDS_PORT=5432
# RDS_USERNAME=postgres
# RDS_DB_NAME=blacklist_db
# AWS_REGION=us-east-1

# AWS Credentials (optional if using IAM roles on EB)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key